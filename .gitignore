__pycache__/
*.pyc
*.egg-info/
.venv/
venv/

# tokenised corpora and raw downloads — regenerate via data/*/prepare.py.
# NOTE: manifest.json IS tracked, so the frozen slice stays verifiable by
# sha256 without committing gigabytes of .bin.
*.bin
data/*/input.txt
data/*/meta.pkl

# checkpoints / run outputs
out/
out-*/
wandb/
# proof_sweep writes per-experiment checkpoints and the generated results
# table here. Commit a results table deliberately if you want it in history.
runs/
